import React from "react";
import ReactDOM from "react-dom/client";
import ImpositionTool from "../imposition-tool";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<ImpositionTool />);
